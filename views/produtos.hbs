<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <title>{{titulo}}</title>

    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #34495e;
            font-size: 2.4rem;
            margin-bottom: 40px;
            letter-spacing: 1px;
            font-weight: bold;
        }

        .card-title strong {
            color: #0d6efd;
            font-weight: 600;
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }

        .card {
            border: 1px solid #e0e0e0;
            border-radius: 10px;
            background-color: #ffffff;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
        }

        .card-body {
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        .card-title {
            margin-bottom: 15px;
            font-size: 1.25rem;
        }

        .card-text {
            font-size: 1.1rem;
            color: #7f8c8d;
            margin-bottom: 15px;
            flex-grow: 1;
        }

        .btn {
            font-size: 0.9rem;
        }

        .alert-warning {
            text-align: center;
            font-size: 1.1rem;
            padding: 15px;
            border-radius: 8px;
            background-color: #fff3cd;
            color: #856404;
        }

        /* Estilo para a imagem do produto */
        .produto-img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 15px;
            transition: transform 0.3s ease;
        }

        .card:hover .produto-img {
            transform: scale(1.1);
        }

        /* Responsividade */
        @media (max-width: 768px) {

            .col-md-6,
            .col-lg-4 {
                width: 100%;
            }

            .card {
                margin-bottom: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1 class="display-5 mb-4">{{titulo}}</h1>

        {{#if produtosParaView.length}}
        <div class="row">
            {{#each produtosParaView}}
            <div class="col-md-6 col-lg-4 mb-3">
                <div class="card h-100">
                    <img src="{{this.imagem}}" alt="Imagem do produto" class="produto-img">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">
                            <strong>{{this.produto}}</strong>
                            <small class="text-muted fw-normal"> (ID: {{this.id}})</small>
                        </h5>
                        <p class="card-text flex-grow-1">{{this.preço}}</p>
                        <a href="#" class="btn btn-sm btn-outline-primary mt-auto">Ver Detalhes</a>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="alert alert-warning" role="alert">
            Nenhum item para exibir no momento.
        </div>
        {{/if}}

        <hr class="my-4">
        <a href="/" class="btn btn-secondary">Voltar para a página inicial</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>